# Handoff — Noetica / prophet-mesh (2026-06-28)

For the agent picking this up. Read this top-to-bottom, then read the memory index (below) before acting.

---

## 0. Current state (TL;DR)
- **Everything this session is merged to `main`** (`origin/main` tip `f0e25e8`), via **PR #284** (the 148-commit
  integration `feat/robust-board-harness → main`, merged by the user) + **PR #285** (two follow-up CI fixes).
- **main CI**: the `validate` workflow was finishing its run on `f0e25e8` at handoff — the *identical* code
  already passed green on PR #285, so it's expected green. **Verify** `gh run list --branch main` shows
  `validate: completed success` for `f0e25e8` before assuming green.
- I am on branch `fix/ci-python-skip-and-runbook` locally; it's merged. Switch to `main` and `git pull`.

---

## 1. Repos & paths (where the work lives)
- **Primary repo: `~/dev/Noetica`** (GitHub `SocioProphet/Noetica`, remote alias `github-443`). Default branch `main`.
  - `agent-machine/` — the local agent server. **This is where ~90% of this session's work is.**
    - `server.ts` — the HTTP server (the `/api/*` routes, the answer path, deliberation).
    - `lib/` — the engine (retrieval, canon, operators, reason-lane, raptor, study-outputs, presets, …).
    - `scripts/` — benches + GCP launchers (`mmlu-brain-bench.ts`, `gcp-board-robust.sh`, `graphrag-bench.py`, …).
    - `canon/spec-*.json` — the **frontier-authored** canon (7 domains). NOT model-generated.
    - `dist/distill-sft.jsonl` — the distillation dataset (built this session, gitignored output).
    - `docs/` — `gcp-batch-runbook.md`, this handoff.
  - `components/`, `app/`, `lib/` (repo root) — the **Next.js frontend** (chat UI, surfaces, voice, SAE panels).
- **Memory: `~/.claude/projects/-private-var-folders-…/memory/`** — `MEMORY.md` (index, auto-loaded) + one file
  per fact. **Read these first; they encode the strategy + standing constraints.**
- **Local data (not in git): `~/.noetica/`** — `corpus/benchmarks/graphrag-bench/` (staged GraphRAG-Bench),
  `corpus/benchmarks/mmlu_stem.json` (the MMLU bank the board reads).
- **Estate repos under `~/dev/`** (model-adjacent): `lattice-forge` (= "Prophet Lattice", governed RUNTIME not
  training), `embeddinglab`, `graphlab`, `imagelab`, `functional-model-surfaces` (governed model/dataset/eval
  standards), `SocioProphet__gaia-world-model`, `SociOS-Linux__speechlab` (voice), `AgenticaForge`, plus
  BearBrowser / TurtleTerm / SourceOS / goose-notes / exodus (see memory).

---

## 2. Where focus has been (this week / past 3 days)
The through-line: **prove and then SURFACE the "harness > model" moat, measured against peers.**
- **Brain + board** (earlier): MMLU board (`scripts/mmlu-brain-bench.ts`), verified operator library
  (`lib/math_operators.py`), reason lane (`lib/reason-lane.ts`), CRAG gate, OCW capture, medicine/legal domain
  brains, academy/registrar. Proven: reason+SC +24pp on computational math; operators 1/6→7/7.
- **Competitive intelligence** (last 3 days): surveyed GraphRAG / LightRAG / FalkorDB / Onyx(=Danswer) / RAGFlow
  / kotaemon / AnythingLLM, then **IBM watsonx (full portfolio) + NotebookLM + LLM model-dev**. Findings in
  memory `competitive-intel-localrag`, `watson-notebooklm-modeldev-audit`.
- **Gap audits → capability map**: pedantic 6-axis audit (`strategy-gap-audit`) then a recon-grounded
  **capability map** (`capability-map`) — the key lesson: *we keep being MORE built than assumed*; the moat is
  PROD-WIRED; the gaps are CONVERSION (ergonomics, surfacing), not capability.

**The single most important strategic finding:** the **verified-compute moat holds even vs IBM watsonx.governance**
— Watson *monitors* models (drift/GRC, post-hoc, cloud); we *prove* computations (cryptographic receipts,
verified operators, local-sovereign). No peer (incl. Onyx, IBM) has verified compute.

---

## 3. What we did TODAY (2026-06-28) — detailed, with paths
All committed to `main`. Each has unit tests + typecheck 0 unless noted.

1. **GraphRAG-Bench integration** — `agent-machine/scripts/graphrag-bench.py`. Stages the field-standard bench
   (medical 2062 + novel 2010 Qs) at `~/.noetica/corpus/benchmarks/graphrag-bench/`; routes question types to
   our arms; `--dry-run` validated routing (99% medical / 67% novel retrieval-overlap with a *strawman* keyword
   retriever — see gap #1 below).
2. **Ergonomics (the #1 audited gap)** — `agent-machine/lib/presets.ts` (+ `presets.test.ts`, 6 tests) collapses
   100+ `NOETICA_*` env knobs into `lite|balanced|max`, RAM-auto-detected, soft memory degradation; wired into
   `server.ts` startup (`applyPreset()` in the `server.listen` callback). `agent-machine/scripts/doctor.ts` —
   one command that surfaces preset + RAM headroom + whether the model is on Metal or fell to CPU.
3. **RAPTOR** — `agent-machine/lib/raptor.ts` (model-agnostic core, 6 tests) + `lib/raptor-runtime.ts` (binds
   `embedBatchLocal` + `generateOllamaText`). Recursive cluster→summarize→collapsed-retrieve. **Built, NOT yet
   wired into the production retrieval path** (see gap).
4. **Provenance UX** — `components/chat/MessageBubble.tsx`: added a **Grounding** chip section surfacing
   `message.grounding` (frontier-authored canon domain/topics/terms) — was generated but rendered nowhere.
   (Most of the provenance panel — verdict badge, deliberation, trace chips — was already shipped.)
5. **NotebookLM-class Studio** — `agent-machine/lib/study-outputs.ts` (model-agnostic core, 6 tests) +
   `lib/study-outputs-runtime.ts` (binds `generateOllamaText` + `canonDef`) + **`/api/studio` endpoint** in
   `server.ts` (briefing / study-guide / audio-script). **Differentiator:** study-guide definitions come from
   the **frontier-authored canon** (`canonDef`), tagged `source:'canon'|'model'` — NotebookLM/Watson can't
   ground definitions authoritatively.
6. **Distillation dataset builder** — `agent-machine/scripts/build-distill-dataset.py` → `dist/distill-sft.jsonl`
   (RAN: 1,342 SFT pairs, 1,337 frontier canon defs + 5 verified-operator outputs, **0 from the local 7B**).
7. **GCP batch runbook** — `agent-machine/docs/gcp-batch-runbook.md` (the 3 GPU jobs; read it).
8. **The merge to main + 2 CI fixes**:
   - Resolved a 6-file conflict (canon specs + `lib/canon-lookup.ts` + `canon-route.ts` + `symbol-query.ts` +
     `scripts/mmlu-brain-bench.ts`) — kept our newer features, **unioned** the canon glossaries (loader dedupes).
   - `lib/exec-verify.test.ts` — skip the real-python operator test when `python3+sympy` absent (CI has no sympy).
   - `scripts/integration-rocks.test.ts` — de-flaked the restart-persistence test (poll, don't race a 500ms wait).

---

## 4. Recommended best path + next tranches
Strategy = master roadmap in memory `total-superiority-roadmap` (3 waves). **Discipline guard: surface+ship the
moat BEFORE adding capability** — this estate's failure mode is "build deeper than we ship smoother."

**Already spawned as worktree tasks (the next tranches):**
- `task_e9adde53` — dual-layer retrieval + RAPTOR. RAPTOR core done; remaining = **point `graphrag-bench.py`
  at the production `retrieve()`** (it currently measures a strawman keyword retriever → the 67% is fake-low),
  and wire RAPTOR into summarize-intent retrieval (tree-build is model-heavy → GCP).
- `task_0f5fd1c1` — provenance UX. Grounding badge done; remaining = **inline `[1]` citations in prose** (needs
  model to emit markers or a post-hoc matcher).
- `task_22463f9c` — **GCP reasoning-model eval** (R1-distill vs qwen reason-lane on conceptual subjects). Job 1
  in the runbook. Highest-signal, cheapest.
- `task_a3e7aa59` — graph-DB backend (FalkorDB/Neo4j, abstracted; in-memory stays default) + auto-KG from user
  docs, **segmented** from the authored canon.
- `task_7d975b76` — **governed connector framework** (GitHub/GDrive/Slack/web, each emitting a provenance
  Receipt) — catches Onyx's 40+ connectors with our governance twist.
- `task_47be9983` — deep doc parsing (pymupdf + Nougat/Marker), fixes the `�` math-glyph corpus junk + matches
  RAGFlow.
- `task_d626afa4` — canon→sovereign-model **QLoRA distillation pilot** (Job 3; dataset already built). Honest
  gate: keep the harness if distillation doesn't beat base+canon-in-context.
- **IN FLIGHT (user started both — RESOLVE THE DUP):** `task_7cfe0d4e` (full NotebookLM Studio UI end-to-end)
  AND `task_95cdbe9a` (tightened UI+audio respawn). They overlap; the engine + `/api/studio` are already on main,
  so these are **frontend + per-speaker TTS** only. Kill one, rebase the other onto current main.

**Recommended order:** (1) confirm main green; (2) fire **runbook Job 1** (reasoning-model eval — cheapest,
clearest open question); (3) resolve the two in-flight Studio UI tasks; (4) Job 2 (GraphRAG-Bench on the REAL
retriever); (5) the platform-breadth tranche (connectors, graph-DB, doc-parsing) per Wave 2.

---

## 5. Pre-identified gaps (from the capability map + audits)
- **Built-not-wired:** RAPTOR (needs an offline, model-heavy tree-build → GCP).
- **Measurement artifact:** GraphRAG-Bench harness uses a strawman retriever, not prod `retrieve()` — our real
  dual-layer has **never been measured** on the field benchmark.
- **Genuinely missing (all spawned):** scalable graph-DB + auto-KG-from-user-docs; governed connectors (Onyx
  parity); deep doc parsing (the `�` junk is `pypdf` failing on math glyphs); inline prose citations; an
  integrated reasoning model (conceptual ceiling); LLM weight-training (deliberate — but canon→distillation is
  the on-thesis path); data lakehouse + enterprise compliance certs (the watsonx.data / FedRAMP gap).
- **Conceptual-math ceiling:** reason lane gains only +3pp on abstract_algebra vs +24pp on computational math —
  topology/analysis/theorem-pair questions need a reasoning model (Job 1).

---

## 6. Standing constraints (DO NOT violate — from feedback memories)
- **Canon/glossary defs are authored by the FRONTIER model, NEVER the local 7B.** The miner discovers terms; the
  frontier authors definitions. (`feedback_glossary_frontier_authored`)
- **No `Co-Authored-By` trailer in git commits.** (`feedback_no_co_authored_by`)
- **Board min n≥30/subject**, seed 1729, clean-eval (never test answers). (`feedback_board_min_n`)
- **Never delete bench arms** (they're measurement); "promote" = wire a winner into the product, gated on
  MEASURED benefit. (`feedback_board_keep_all_promote_winners`)
- **Spec-first**: read sourceos-spec, conform to canonical schemas before building. (`feedback_spec_first_conformance`)
- Cannot make IAM/access-control/repo-settings changes even when authorized. PR-only to `main`; let CI go green
  before merging a release branch (a guard caught 2 real failures this session — don't bypass it).
- For `gcloud`/`gsutil` on macOS: prepend `OBJC_DISABLE_INITIALIZE_FORK_SAFETY=YES`.

---

## 7. Open loops / watch items
- **Confirm main `validate` CI is green** on `f0e25e8` (was in_progress at handoff).
- **Two in-flight Studio UI worktree tasks** (`task_7cfe0d4e`, `task_95cdbe9a`) — dedupe + rebase.
- **8 GB M2 box is the constraint**: it thrashes/CPU-falls under a 7B + sympy + best-of-N. Use `lite` preset
  locally; all heavy runs (reasoning model, RAPTOR tree-build, QLoRA, full boards, GraphRAG-Bench gen+judge)
  are **GCP-shaped** — see the runbook, use `scripts/gcp-board-robust.sh` (resumable, GCS checkpoints).
- **Memory is the source of truth for strategy** — `MEMORY.md` lists: `total-superiority-roadmap`,
  `capability-map`, `strategy-gap-audit`, `competitive-intel-localrag`, `watson-notebooklm-modeldev-audit`,
  plus the domain/brain/board/feedback files. Update them as you go.
