# Verified-compute spine for the agent-machine.
# These are HARD runtime deps of the operator runtime (lib/math_operators.py,
# lib/eval_sympy.py) and the verified-vs-raw / mmlu benchmarks — the "compute arm"
# executes equations with these instead of the model cold-translating.
#
# The exec-verify end-to-end test (lib/exec-verify.test.ts) shells out to `python3`
# and imports math_operators, which imports sympy. If sympy is absent the pre-push
# gate (npm test) fails — provision with:  pip3 install -r agent-machine/requirements.txt
#
# Install on PEP-668 (Homebrew/Debian) systems: add --break-system-packages, or use a venv.
sympy==1.14.0
numpy

# Used by the corpus/benchmark/induction scripts under agent-machine/scripts/ (not the pre-push gate, but
# undeclared deps here are the same trap sympy was — declare so a fresh box / CI provisions cleanly).
scipy            # symbolic/numeric helpers (model_solve, chain_solve, units)
scikit-learn     # clustering + RF meta over corpus vectors (cluster_analysis, rf_meta, domain_softmax)
pypdf            # PDF text extraction (extract_pdfs, marker-extract)
jsonschema       # artifact/proof validation (validate_proof_artifact)
gplearn          # symbolic regression (symreg) — optional; only the symreg script needs it

